[project]
name = "relus-skills"
version = "0.1.0"
description = "Reusable Agent Skills maintained by relus-cy"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "relus-cy"}]
dependencies = []

[project.urls]
Repository = "https://github.com/relus-cy/skills"
