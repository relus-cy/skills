# skills

Reusable Agent Skills maintained by `relus-cy`.

## Included skills

- [`dsh-doc-audits`](skills/dsh-doc-audits/) — bootstrap, migrate, synchronize, and audit repository documentation governance using a portable adaptation of the DeepSeek Harness documentation model.

## Status

`dsh-doc-audits` is at `0.1.0` and uses only Python's standard library for deterministic checks.
